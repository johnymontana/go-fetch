# Dgraph Configuration
# Single port format (gRPC port, HTTP port auto-derived as gRPC-1000)
DGRAPH_CONNECTION_STRING=dgraph://localhost:9080
# Or explicit format with both gRPC and HTTP ports
# DGRAPH_CONNECTION_STRING=dgraph://localhost:9080,8080

# AI Provider Configuration (choose one)
# OpenAI
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Vercel AI SDK Provider (openai, anthropic, etc.)
AI_PROVIDER=openai

# Embedding Model Configuration
EMBEDDING_MODEL=text-embedding-3-small
LLM_MODEL=gpt-4o-mini